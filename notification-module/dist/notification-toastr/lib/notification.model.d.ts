export declare class Notification {
    title: string;
    type: string;
    msg: string;
    show: boolean;
    timeout: number;
    constructor(title: string, type: string, msg: string, show: boolean, timeout: number);
}
